import React, {Component} from 'react';

class TempForm extends Component {
  render() {
    return (

    );
  }

  _handleSubmit(event) {
    event.preventDefault();

  }

  this._min.value = '';
  this._max.value = '';
  this._risk.value = '';

}